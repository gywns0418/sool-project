<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.sool.mapper.ReportMapper">

    <!-- 신고 사유 가져오기 -->
     <select id="getReportReasons" resultType="com.example.sool.dto.CommonCodeDto">
        SELECT
            code,
            code_name
        FROM common_code
        WHERE group_code = 'REPORT_REASON'
        AND use_yn = 'Y'
        ORDER BY sort_seq
     </select>

    <!-- 신고 등록 -->
    <insert id="insertReport" parameterType="com.example.sool.dto.ReportDto">
        INSERT INTO report (
            user_id,
            obj_id,
            obj_type,
            reason_code,
            status_code,
            created_at
        )
        VALUES (
            #{userId},
            #{objId},
            #{objType},
            #{reasonCode},
            'RECEIVED',
            NOW()
        )
    </insert>

    <!-- 신고 존재 확인 -->
    <select id="existsReport" parameterType="com.example.sool.dto.ReportDto" resultType="int">
        SELECT COUNT(*)
        FROM report
        WHERE user_id = #{userId}
        AND obj_id = #{objId}
        AND obj_type = #{objType}
        AND is_deleted = 'N'
    </select>

    <!-- 신고 단건 조회 -->
    <select id="selectReportById" parameterType="int" resultType="com.example.sool.dto.ReportDto">
        SELECT *
        FROM report
        WHERE report_id = #{report_id}
        AND is_deleted = 'N'
    </select>

    <!-- 신고 전체 조회 -->
    <select id="selectReportList" resultType="com.example.sool.dto.ReportDto">
        SELECT *
        FROM report
        WHERE is_deleted = 'N'
        ORDER BY report_id DESC
    </select>

    <!-- 신고 상태 수정 -->
    <update id="updateReportStatus" parameterType="com.example.sool.dto.ReportDto">
        UPDATE report
        SET
            status_code = #{status_code},
            updated_at = NOW()
        WHERE report_id = #{report_id}
    </update>

    <!-- 신고 삭제 -->
    <update id="deleteReport" parameterType="int">
        UPDATE report
        SET
            is_deleted = 'Y',
            deleted_at = NOW()
        WHERE report_id = #{report_id}
    </update>

</mapper>